services:
  - type: web
    name: smart-9
    env: python
    runtime: python-3.12   # ← version Python 3.12
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
